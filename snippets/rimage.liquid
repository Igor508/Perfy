<img
  class="{{ img_class }}"
  alt="{{ image.alt }}"
  src="{{ image | image_url: width: 125 }}"
  width="{{ image.width }}"
  height="{{ image.height }}"
  srcset="{% render 'srcset', image: image %}"
  loading="lazy"
>